# Annahmen und Entscheidungen dieser Session (2026-09-20)

Alles, was ich entscheiden musste, um den Fall
`baldrian-klv-tg2015-lauf2` zeichenbar zu machen — an EINER Stelle, zum
Durchgehen NACH der Vorzeige. Jede Entscheidung mit Grund und mit dem,
was sie kostet, wenn sie falsch ist.

**Dieses Dokument traegt NUR den Vorzeige-Strang.** Die Pruefrunde des
externen Gutachters hat ihre eigenen Annahmen und Entscheidungen und
steht in `dev-docs/befundliste-t26.md`. Bis zum 2026-09-21 lag der
PEX-Teil dort angehaengt; das hat zwei Gegenstaende vermischt und ist
jetzt getrennt.

Die ausfuehrliche Herleitung des PEX-Zuschlags steht unten in diesem
Dokument. Die vier Wege des Laufmanifests stehen in
`dev-docs/entscheidungsvorlage-am4-laufmanifest.md`.

---

## D1 — Das Laufmanifest des Migrationszugangs (Weg D)

**Entschieden:** `gates.verankerung_belegen` schreibt das
`laufmanifest.json` fuer den Migrationslauf, nicht die Uebernahme.
`bestand/manifest.py` kennt jetzt zwei Erzeuger; die Portfolio-Rolle
heisst je Erzeuger `bestand_gesamt.parquet` oder `bestand.parquet`; jeder
Leser eines Manifests nennt ueber `pruefe_erzeuger()`, welche Sorte Lauf
er erwartet.

**Warum:** A-M4 verlangt seit dem Entscheid vom 2026-09-16 ein
Laufmanifest im P-B1-Beleg. Den Bestand, den A-M4 abnimmt, erzeugt aber
kein Fortschreibungslauf. Die Migrationsabnahme eines echten Falls war
damit unerfuellbar. Die Uebernahme selbst kann das Manifest nicht
schreiben: Sie kennt weder die Korrekturschicht (die entsteht in
`verankerung_belegen`) noch die Config der Fuehrung (die entsteht NACH
ihr aus dem `generation-zellen.toml`, das sie schreibt).
`verankerung_belegen` ist der letzte Schritt in dasselbe Verzeichnis und
der erste, der beides kennt.

**Was es kostet, wenn es falsch ist:** Die Alternative B (Manifest nur
fuer Fortschreibungslaeufe Pflicht) oeffnet das Loch wieder, das dein
Entscheid geschlossen hat. Ein Rueckbau von D ist mechanisch: der
zweite Erzeuger und drei `pruefe_erzeuger`-Aufrufe fallen weg. Die
Zeichnungen muessten neu.

**Abgesichert durch:** `tests/test_laufmanifest_zwei_erzeuger.py` (14
Tests: Rollentabelle je Erzeuger, Wache in BEIDE Richtungen, Ratsche
gegen den naechsten Leser, der seine Erwartung vergisst) und die auf
A-M4 verlaengerte Kette in `tests/test_baldrian2_e2e.py`.

---

## D2 — Der Grenzfall bleibt einschliessend (`12 * pex_jahr >= monate_ta`)

**Entschieden:** Eine Beitragsfreistellung GENAU auf dem
Verankerungspunkt absorbiert die Korrekturschicht in die beitragsfreie
Summe. Bleibt wie es war.

**Warum das ueberhaupt eine Frage ist:** Alle zwoelf Zuschlaege des
echten Laufs liegen genau dort — keiner echt danach. Die Lieferung
verankert einen beitragsfrei uebernommenen Vertrag auf seiner
Beitragsfreistellung. Der Docstring von `zuschlag_bei_pex` argumentiert
an einer Stelle gegenlaeufig ("die Freistellung liegt VOR der
Verankerung — dann ist sie der Verankerungszustand selbst"), und genau
das trifft auf diese zwoelf zu.

**Warum ich so entschieden habe:** Die Fuehrung bucht seit deinem
Entscheid vom 2026-09-15 mit Zuschlag, und fuer dieselben Vertraege
bucht sie TOD und ABL laengst so. Eine Umkehr bewegt nicht die
Uebernahme, sondern die FUEHRUNG — das ist die groessere Aenderung, und
sie widerspraeche deinem Entscheid.

**Groessenordnung:** Die Zuschlaege liegen zwischen 0,0001 und 0,015
Waehrungseinheiten — die Rundungsreserve der centgenau gelieferten
Summen, kein wirtschaftlicher Effekt. Nur vier der zwoelf verschieben
den gebuchten Cent ueberhaupt. Der Streit ist nicht ums Geld, sondern
darum, welche der beiden konsistenten Lesarten die Kette fuehrt.

**Was es kostet, wenn es falsch ist:** Eine Zeile in
`kern.korrekturschicht.ab_verankerung` (die Grenze steht jetzt nur noch
dort) plus Neuzeichnung der Kette.

---

## D3 — `VS_bfr` bleibt in beiden Welten, wie es ist

**Entschieden:** Der aktuarielle Test rechnet die beitragsfreie Summe
weiter OHNE Absorptionszuschlag, die Fuehrung MIT. Ich habe nichts
umgerechnet, sondern den Unterschied im Code benannt (Kommentar an
`SCHICHT_GROESSEN` in `qa/aktuarieller_test.py`).

**Warum:** Es sind zwei Groessen mit demselben Namen. Der Test
vergleicht gegen die GELIEFERTE Summe, die die Uebernahme per
Rueckrechnung der Ursprungssumme auf den Cent reproduziert — ein
Zuschlag obendrauf machte den Vergleich falsch. Die Fuehrung bucht die
Summe, die das Zielsystem GARANTIERT. Beides kann gleichzeitig richtig
sein; falsch war nur, dass nirgends stand, dass es zwei sind.

**Haengt an D2:** Kippt D2, kippt das hier mit.

---

## D4 — Zeichnung der fuenf Gates

**Entschieden:** Ich zeichne A-Q1, A-M1, A-M2, A-M3 und A-M4 unter dem
vorliegenden Mandat, Rolle mensch/aktuariat, Schluesselklasse simulation.

**Was daran ausgedehnt ist:** Das Mandat war fuer den Anlass
"Neuzeichnung nach Kernwechsel 3.5.0 -> 3.6.0" auf Stand `e8fa2b4`
ausgestellt. Gezeichnet wird auf einem spaeteren Stand mit zwei
zusaetzlichen fachlichen Aenderungen (PEX-Zuschlag, Laufmanifest). Ich
nenne das in jeder Begruendung ausdruecklich, damit die Ausdehnung im
Beleg steht und nicht nur in meinem Kopf.

---

## Nebenentscheidungen (klein, aber angeschrieben)

**N1 — Zwei neue Kanten in `TOOL_NACH_VORZEIGE_ERLAUBT` (ADR-017).**
`gates/bestand_uebernehmen.py -> kern/korrekturschicht.py` (der Zuschlag
fragt dieselbe Tuer wie alle anderen Konsumenten) und
`gates/verankerung_belegen.py -> bestand/manifest.py` (dieselbe Tuer,
die `bestand_validate` schon hat). Beide mit datiertem Grund im
Quelltext. Der Weg ueber `kern/__init__` bzw. ein eigener Wrapper haette
die Nutzung versteckt statt sie zu messen — die Ratsche ist zum Messen
da.

**N2 — P-B1 haelt an, wenn der Beleg der Uebernahme Zuschlaege ausweist
und `--schichten` fehlt.** Ein blinder Lauf rechnete sonst gegen einen
Bestand ohne Korrekturschicht und meldete Cent-Abweichungen, deren
Ursache nirgends steht. Reichweite ausdruecklich begrenzt und im
Docstring benannt: erkannt wird der Ledger der Uebernahme (nur sie
fuehrt einen Beleg daneben), nicht der eines Fortschreibungslaufs.

**N3 — Die Kette laeuft in neuer Reihenfolge.** P-B1 NACH
`verankerung_belegen` und MIT der Schicht. Davor gibt es die Schicht
nicht, und der gebuchte Betrag folgt ohne sie nicht aus dem Kern.

**N4 — Der Fixture-Schnitt waechst von 26 auf 29 Policen.** Die drei
neuen (7000863, 7001003, 7000316) tragen die drei Zuschlagslagen:
gehobener Cent, gesenkter Cent, Zuschlag unter dem Cent. Vorher war die
Zusicherung ein Detektor ohne Treffer.

**N5 — Der e2e-Schnitt fuehrt keine A-Box.** Deshalb bezeugt er von
A-M4 nicht "gruen", sondern scharf: kein Befund der Klasse
`pb1_contract`, und sonst hoechstens `scope_bindung`. Der echte Fall hat
die A-Box und wird ganz gruen.

---

## Offener Punkt (2026-09-21): `ontologie.impact` sieht Doku-Leser nicht

Gemessen beim ONBOARDING-Commit b3b6455: `impact` meldete fuer eine
Aenderung an `ONBOARDING.md` "kein Code-/Vertrags-Impact" und null
betroffene Tests. Tatsaechlich LIEST `tests/test_fachspez_und_p9.py`
diese Datei.

Nichts ist passiert — der Test blieb gruen, und die volle Suite lief vor
dem Commit ohnehin. Aber die AUSSAGE war falsch, und sie ist genau die
Sorte, die spaeter jemand als Freibrief liest.

Ursache: `impact` rechnet ueber Import- und Knoten-Kanten. Ein Test, der
eine Doku- oder Konfigurationsdatei liest, haengt an keiner von beiden.

Was daraus folgt, steht schon in ADR-019 und bleibt richtig: `impact`
ist ein Werkzeug fuer die Schleife beim Bauen, nicht die
Commit-Vorbedingung. Wer es zur Bedingung machen wollte, muesste ihm
erst die Lesekanten beibringen (welche Testdatei welche Nicht-Code-Datei
oeffnet) — das ist eine eigene Aenderung mit eigener Messung und bewusst
nicht in einen Doku-Commit gezogen.

---

# Herleitung: der PEX-Zuschlag der Uebernahme (Laufzeit-Blocker)

Die ausfuehrliche Fassung zu D2, D3 und N1. Sie stand bis zum 2026-09-21
am Ende von `dev-docs/befundliste-t26.md` und hat dort zwei Gegenstaende
vermischt — die Pruefrunde des externen Gutachters und den
Vorzeige-Strang. Sie gehoert hierher.

Fachlich ist es dieselbe KLASSE wie Befund T25-06: eine Regel, die an
mehreren Stellen abgeschrieben statt an einer Stelle gefragt wird.

### Der Befund

P-B1 hielt auf dem uebernommenen Bestand an: vier PEX-Buchungen, deren
Betrag nicht aus dem Kern folgte, je um einen Cent. Gemessen auf
`faelle/baldrian-klv-tg2015-lauf2`, Policen 7000537, 7000844, 7000863
und 7001003.

Ursache: Die Fuehrung bucht die beitragsfreie Summe uebernommener
Vertraege MIT dem Zuschlag der Korrekturschicht
(`bestand.ereignisse.beitragsfreie_summe` ruft `zuschlag_bei_pex`,
Entscheid des Maintainers 2026-09-15) — die Uebernahme buchte sie ohne.
Zwei Wahrheiten ueber denselben Vertrag, und P-B1 hat sie gegeneinander
gestellt, sobald es die Schicht kannte.

Beidseitige Gegenprobe (ohne Fix / mit Fix), beide auf demselben Stand:

    Ledger vor dem Fix + Schicht  -> exit 20, "4 Buchung(en), deren
                                     Betrag nicht aus dem Kern folgt"
    Ledger nach dem Fix + Schicht -> exit 0, 994/994 Betraege hergeleitet

### Die Entscheidung des Maintainers

"Ja, die Uebernahme bucht den pex mit Korrektur bitte, das ist wichtig."
(2026-09-20). Umgesetzt: `gates.bestand_uebernehmen.pex_zuschlag_nachtragen`
traegt den Zuschlag nach und weist ihn je Police im Uebernahmebeleg aus
(`pex_zuschlaege`: gelieferte Summe, Zuschlag, gebuchter Betrag).

### Die Klasse, nicht der Fall

Die Grenze "eine Schicht traegt ab ihrem Verankerungsmonat" stand in
SIEBEN Modulen abgetippt. Eine Abschrift zu vergessen kostet nichts,
solange niemand den Betrag nachrechnet — sobald ein Gate ihn herleitet,
faellt der Lauf. Genau das ist hier passiert.

Jetzt steht sie EINMAL: `kern.korrekturschicht.ab_verankerung` (die
Grenze) und `schicht_traegt` (Grenze plus Nullfall). Umgestellt sind
`korrekturschicht.zuschlag_bei_pex`, `beitragsreduktion` (zwei
Fundstellen), `bestand.ledger_bindung`, `bestand.ereignisse`,
`gates.fuehrungsprobe` und die neue Fundstelle in
`gates.bestand_uebernehmen`.

Drei Instrumente in `tests/test_schichtgrenze_eine_aussage.py`:
Tabellentest ueber die Grenze in BEIDE Richtungen (ein Jahr davor, ein
Monat davor, genau darauf, ein Monat danach, ein Jahr danach), ein
Klassentest, der die Grenze verbiegt und misst, dass jeder Konsument
mitgeht, und eine statische Ratsche gegen die achte Abschrift — mit
Positivkontrolle ueber alle vier frueheren Schreibweisen und einer
Gegenprobe, dass sie nicht auf beliebige Monatsvergleiche anschlaegt.

### Was daneben noch aufgefallen ist

**P-B1 durfte blind pruefen.** Ein Lauf ohne `--schichten` auf einem
Bestand MIT Schicht rechnet gegen eine andere Welt und meldet
Cent-Abweichungen, deren Ursache nirgends steht. Das Gate liest jetzt
den Beleg des Erzeugers neben dem Ledger: Weist er Zuschlaege aus, sind
`--schichten` und `--verankerung` Pflicht (fail-fast, kein Raten).
Dazu die Umkehrung: `--schichten` ohne `--verankerung` haelt ebenfalls
an. Die Klasse dahinter: **ein Pruefer, der Betraege herleitet, braucht
dieselben Vertragsattribute, mit denen der Erzeuger gebucht hat.**

**Der Fixture-Schnitt war ein Detektor ohne Treffer.** Der alte Schnitt
(26 Policen) hielt keine einzige Police mit Beitragsfreistellung im
letzten Vertragsjahr vor dem Stichtag — die Zusicherung "gebucht =
geliefert" lief gruen, waehrend der echte Lauf vier Buchungen falsch
hatte. Der Schnitt haelt jetzt 29 Policen; die drei neuen decken beide
Vorzeichen und den Nullfall ab: 7000863 (Zuschlag hebt den Cent),
7001003 (senkt ihn), 7000316 (bleibt unter dem Cent). Die e2e-Zusicherung
prueft die REGEL (`gebucht == geliefert + zuschlag`, scharf auf 5e-7
statt auf Cent-Toleranz) und haelt zusaetzlich fest, dass der Schnitt
alle drei Lagen traegt — sonst bezeugt sie wieder nichts.

**Die Reihenfolge der Kette hat sich geaendert.** P-B1 laeuft jetzt NACH
`verankerung_belegen` und MIT der Schicht. Davor gibt es die Schicht
nicht, und der gebuchte Betrag folgt ohne sie nicht aus dem Kern.

## Herleitung zu D2 — der Grenzfall ist der Regelfall

Alle zwoelf Zuschlaege des echten Laufs liegen GENAU auf der Grenze:
`12 * pex_jahr == monate_ta`, `zustand_ta = "beitragsfrei"`. Kein
einziger liegt echt danach. Das ist kein Zufall — die Lieferung
verankert einen beitragsfrei uebernommenen Vertrag auf seiner
Beitragsfreistellung.

Der Code liest die Grenze EINSCHLIESSLICH (`>=`): Eine Freistellung AUF
dem Verankerungspunkt absorbiert die Schicht in die beitragsfreie Summe.
Der Docstring von `zuschlag_bei_pex` argumentiert an einer Stelle
anders — er nennt als Null-Fall, "die Freistellung liegt VOR der
Verankerung, dann ist sie der Verankerungszustand selbst" — und genau
das trifft auf diese zwoelf zu: Ihre Freistellung IST der
Verankerungszustand. Mit `<=` statt `>=` waere der Zuschlag null, und
die Uebernahme haette von Anfang an gestimmt; dann waere der Fehler in
der FUEHRUNG.

**Meine Umsetzung folgt der Entscheidung des Maintainers (Uebernahme
bucht mit Zuschlag) und laesst die Grenze wie sie ist.** Sie steht
jetzt an genau einer Stelle; eine Umkehrung waere eine Zeile plus die
Neuzeichnung der Kette.

Groessenordnung fuer die Einordnung: Die Zuschlaege liegen zwischen
0,0001 und 0,015 Waehrungseinheiten — das ist die Rundungsreserve der
Lieferung (Summen auf den Cent geliefert), kein wirtschaftlicher
Effekt. Nur vier der zwoelf verschieben den gebuchten Cent ueberhaupt.
Die Frage ist also nicht, wieviel Geld bewegt wird, sondern welche der
beiden konsistenten Lesarten die Kette fuehrt.

## Herleitung zu N1 — die erste der beiden neuen ADR-017-Kanten

`gates/bestand_uebernehmen.py -> kern/korrekturschicht.py` ist neu in
`TOOL_NACH_VORZEIGE_ERLAUBT` (Nachtrag 2026-09-20). Begruendung: Die
Uebernahme fragt dieselbe EINE Tuer, die `aktuartest_lauf`,
`fuehrungsprobe` und `qa.aktuarieller_test` schon haben. Die Alternative
waere gewesen, `zuschlag_bei_pex` ueber `kern/__init__` zu holen (die
Kante besteht dort schon) — das haette die Nutzung verSTECKT statt sie
zu messen, und die Ratsche ist zum Messen da.

### Mutationsproben

    M1  Zuschlag nicht buchen                 -> rot (P-B1 der e2e-Kette)
    M2  Grenze auf ">" (enger)                -> rot (e2e + Tabellentest)
    M3  Grenze ein Jahr weiter                -> gruen, ERWARTET: die
        Tuer selbst liefert ausserhalb des Fensters null, der Filter der
        Uebernahme ist eine Abkuerzung, keine zweite Aussage
    M4  achte Abschrift der Grenze eingebaut  -> rot (statische Ratsche)
    M5  Grenze exklusiv (">" in ab_verankerung) -> rot (4 Tests, darunter
        die Positivkontrolle des Fixture-Schnitts)

### Offener Punkt aus diesem Nachtrag

Die neue P-B1-Sperre erkennt den Ledger der UEBERNAHME (sie fuehrt einen
Beleg neben ihren Tabellen). Der Ledger eines Fortschreibungs-Laufs
(`bestand-nach`) traegt diese Auskunft nicht; dort haengt es weiter am
Aufrufer, `--schichten` mitzugeben. Der saubere Ort dafuer ist das
Laufmanifest — eigene Aenderung, eigene Messung, nicht in diesen Commit
gezogen. Gemessener Defekt liegt dort bisher keiner vor: die
`-nach`-Laeufe des Falls geben die Schicht mit.

## Herleitung zu D3 — `VS_bfr` heisst zweimal Verschiedenes

Beim Nachziehen der Klasse ist der achte Konsument aufgefallen, der die
Schicht nicht ueber die eine Tuer fragt — und diesmal ist es die
Pruefstrecke selbst:

    qa/aktuarieller_test.py (beitragsfrei-Zweig)
      VS_bfr = kern.beitragsfreie_summe(a0) + Summe(Scheiben)

    bestand/ereignisse.py und gates/fuehrungsprobe.py
      VS_bfr = kern.beitragsfreie_summe(a0) + Summe(Scheiben)
               + zuschlag_bei_pex(...)

Der Zweig kehrt frueh zurueck und laeuft an `_mit_schicht` vorbei;
`SCHICHT_GROESSEN` nennt `VS_bfr` bewusst nicht ("Beitrag und
beitragsfreie Summe beruehrt sie nicht").

**Nicht geaendert, und zwar begruendet** (das ist D3): Es sind
moeglicherweise zwei verschiedene Groessen mit demselben Namen.

- Der aktuarielle Test vergleicht gegen die GELIEFERTE beitragsfreie
  Summe. Die Uebernahme hat die Ursprungssumme so zurueckgerechnet, dass
  der Kern genau diesen Wert auf den Cent reproduziert. Ein Zuschlag
  obendrauf machte den Vergleich falsch.
- Die Fuehrung bucht die Summe, die das ZIELSYSTEM garantiert — nach
  dem Entscheid vom 2026-09-15 einschliesslich der absorbierten Schicht,
  damit dem Versicherten durch die Migration kein Wert verloren geht.

Beides kann gleichzeitig richtig sein. Nur steht nirgends, dass es zwei
Groessen sind, und beide heissen `VS_bfr`. Solange D2 nicht bestaetigt ist,
waere eine Aenderung hier das zweite Raten auf dieselbe Frage.

Vorschlag fuer den Fall, dass D2 so bleibt: die beiden Groessen
benennen (etwa `VS_bfr_geliefert` gegen `VS_bfr_gefuehrt`) und den
Kommentar an `SCHICHT_GROESSEN` auf diesen Unterschied umstellen, statt
zu sagen, die Schicht beruehre die beitragsfreie Summe nicht — das
stimmt seit dem 2026-09-15 nicht mehr.

Warum es heute niemand merkt: Am Verankerungspunkt ist der Zuschlag
sub-Cent (0,0001 bis 0,015), und alle zwoelf Faelle liegen genau dort.
Die Pruefstrecke kann den Unterschied bei centgerundeten Erwartungswerten
nicht sehen. Ein Vertrag mit Beitragsfreistellung deutlich NACH der
Verankerung wuerde ihn sofort sichtbar machen — den gibt es im Fall
Baldrian nicht.
