# Review-Befunde Lauf-2-Korrekturkette (2026-09-02)

Adversarialer Workflow-Review der 23 Korrektur-Commits auf HEAD 5983417:
fuenf thematische Reviewer, je Befund zwei unabhaengige Skeptiker
(Korrektheits- und Kontext-Linse; bestaetigt = 0 widerlegt, strittig = 1,
verworfen = 2). Bilanz: 22 Rohbefunde, 11 bestaetigt, 9 strittig,
2 verworfen. Fix-Status je Befund unten; Nacharbeit 2026-09-02: alle 11
bestaetigten Befunde gefixt, S1/S5/S6/S8/S9 dabei miterledigt,
S2/S3/S4/S7 als offene Punkte uebernommen (dev-docs/offene-punkte.md).

## Bestaetigte Befunde

### B1 [kritisch/korrektheit] Terminal-PEX in Serien: Ursprungssumme aus nur EINEM (Grund-)Faktor, Scheiben-Historie komplett ignoriert

Block serien-ableitungen; `src/rechner_pipeline/gates/migrationssuite_lauf.py:218`; Status: ERLEDIGT 7c8dfb2 (Begruendung + Aequivalenzgroesse + Zonen-Beleg; Backlog: echte Scheiben-Darstellung nach PEX)

_serienzustand()'s PEX-Zweig (migrationssuite_lauf.py:210-220, identisch in aktuartest_lauf.py) prueft nur, dass PEX das letzte Ereignis der Serie ist, und ruft dann leite_pex_ursprungssumme_ab(mp_felder, pex_jahr=pex_jahr, vs_bfr=erlsumme) mit der VOLLEN gelieferten erlsumme auf -- alle vorangegangenen ERH/RED-Ereignisse der Serie werden fuer die Wertbildung komplett verworfen, es gibt keinen scheiben-Split. leite_pex_ursprungssumme_ab (migrationszugang.py:597) dividiert die gelieferte Summe durch GENAU EINEN Beitragsfrei-Faktor, der aus den Grund-Modellpunkt-Parametern (Alter x, Laufzeit n des GRUNDvertrags) berechnet wird. Die Commit-Begruendung in f9af1cc behauptet 'die beitragsfreien Faktoren aller Bausteine desselben Ablauftermins sind identisch' und stuetzt das nur auf EINEN nachgerechneten Liefervertrag. Nachrechnung mit dem Standard-Modellpunkt KLV_DEFAULT (x=45,n=30,t=20) und einer per erhoehungs_scheibe() um 5 Jahre versetzten Scheibe (x=50,n=25,t=15, gleicher Ablauftermin x+n=75): Rechenkern(grund).beitragsfreie_summe(8) = 0.3925, Rechenkern(scheibe).beitragsfreie_summe(8-5=3) = 0.1912 -- Faktor-2-Abweichung trotz 'desselben Ablaufterrmins'. Bei jedem Fall mit spuerbarem Alters-/Laufzeitabstand zwischen Grund und einer Erhoehungsscheibe vor einer terminalen Beitragsfreistellung liefert dieser Pfad eine falsche Ursprungssumme -- ohne jeden Vorwaerts-Selbstcheck (anders als leite_serie_aus_satz_ab, dessen 'Vorwaertsprobe hart' zumindest die ERLSUMME reproduziert). Der einzige Test dazu (test_anfangszustand_serie_mit_terminalem_pex_ist_einpunkt) konstruiert vs_bfr_geliefert selbst nur aus dem Grund-Faktor (10000*faktor(grund,8)) und kann die Invarianz-Annahme daher gar nicht pruefen -- er ist zirkulaer zur Implementierung.

### B2 [kritisch/stiller-fehler] Reduktion + schicht_conv umgeht den Engine-Waechter komplett (verifiziert)

Block at-engine; `src/rechner_pipeline/qa/aktuarieller_test.py:539`; Status: ERLEDIGT 009464d

_pruefe_auftrag prueft die dokumentierte Unvertraeglichkeit 'Herabsetzung als Anfangszustand + Korrekturschicht' nur ueber `v.schicht is not None` (Zeile 539), nie ueber `v.schicht_conv`. Reproduziert: Vertragspruefung(reduktion=(10,0.6), schicht_conv=<Schichtparameter schichttyp='conv'>, monate_t0=0, ...) durchlaeuft _pruefe_auftrag OHNE AktuartestFehler. In _system_werte (Zeile ~908, Kommentar 'Keine Schicht dazu -- die Kombination weist _pruefe_auftrag zurueck') wird bei reduktion is not None NIE _schichtwert/_mit_schicht aufgerufen -- die registrierte Korrekturschicht wird komplett und unbemerkt aus dem Ergebnis entfernt statt den dokumentierten Hard-Fail auszuloesen. In gates/aktuartest_lauf.py::_schicht_fuer (Korrektur 5, acefbd8) wird ein solcher Eintrag nur dann verworfen (schicht_ausgelassen), wenn dem Vertrag zugleich ein Plausibilitaets-Beleg gewaehrt wurde; ohne Beleg bleibt der schicht_conv-Eintrag stehen und faellt durch die luecke. Getroffene Policen: jede mit Herabsetzungs-Anfangszustand, GETRENNT registrierter conv-Schicht (9.13-Format, aktiv seit Lauf 2) und OHNE gewaehrten Plausibilitaets-Ersatz. Kein Test im Repo konstruiert reduktion zusammen mit schicht_conv (grep ueber alle Tests: 0 Treffer) -- weder der Guard noch die _schicht_fuer-Entscheidungskette pruefen diesen Zweig.

### B3 [kritisch/korrektheit] RED-Folgestichtag im PLV-Verfahren ignoriert dk_am_jahrestag

Block suite-gates; `src/rechner_pipeline/qa/migrationssuite.py:812`; Status: ERLEDIGT 54f0a64 (mit S6)

Alle anderen dk_stichtag_2-Zweige (Zeilen 824, 828-830, 832-837, 839-840) rechnen mit dk_monat_2 = _dk_monat(v.monate_stichtag_2) (Jahrestags-Konvention bei dk_am_jahrestag=True). Der Zweig fuer 'red_monat is not None and reduziert is not None' (mid-period RED unter Verfahren prospektiv/mit_abzug) ruft stattdessen `reduziert.monatsreserve(v.monate_stichtag_2)` mit dem rohen Kalendermonat auf. Beispiel: Vertrag mit RED bei Monat 120 (anteil=0.6, Verfahren prospektiv), stichtag_2 bei Monat 125 (5 Monate nach dem Vertragsjahrestag) und dk_am_jahrestag=True (Quellsystem liefert 'DECKKAP Jahrestag', Ausweitung Nr. 18). Erwartet wird der Wert bei Monat 120, verglichen wird aber Monat 125 -- ein Residuum in Hoehe des Reservezuwachses von 5 Monaten (der exakt gleiche Fehlermechanismus, den test_dk_am_jahrestag_ist_lieferungseigenschaft fuer den einfachen Fall als '> 100 EUR' belegt). Kein Test kombiniert dk_am_jahrestag=True mit einem RED-GeVo im Pruefzeitraum, daher bleibt der Fehler unentdeckt, solange der Fall entweder keine dk_am_jahrestag-Vertraege oder keine PLV-RED-Vertraege mit Jahrestag-Versatz > 0 traf.

### B4 [mittel/korrektheit] Teilkuendigung im beitragsfreien Nachlauf (t..n) hart blockiert

Block kern-verfahren; `src/rechner_pipeline/kern/beitragsreduktion.py:212`; Status: ERLEDIGT b99cbbc (Kern 3.4.0)

reduziere() ruft fuer ALLE drei Verfahren dieselbe _pruefe_eingaben() auf; deren Wache `if jahr >= mp.t: raise ... 'die Beitragszahlungsdauer ist beendet ... es gibt keinen Beitrag zu reduzieren'` ist fuer PROSPEKTIV/MIT_ABZUG korrekt (dort wird tatsaechlich ein laufender Beitrag gesenkt), passt aber inhaltlich nicht auf TEILKUENDIGUNG: die neue Verfahrensvariante kuendigt einen Anteil der GRUNDVERSICHERUNGSSUMME mit Auszahlung (Bedingungswerk Ziffer 6) und hat mit einem laufenden Beitrag nichts zu tun -- bjb_neu ist dort nur informativ. KLV_DEFAULT selbst hat n=30, t=20 (Standardform: Beitragszahlung endet vor Vertragsablauf), also einen zehnjaehrigen Nachlauf, in dem eine Teilkuendigung des Grundbausteins nach Ziffer 6 aktuariell plausibel waere, hier aber ausnahmslos mit der (fuer diesen Fall sachfremden) Beitrags-Fehlermeldung abgewiesen wird. Der 2. Lauf hat das nicht gezeigt, weil alle neun A-M3-RED-Faelle offenbar in der beitragspflichtigen Phase lagen.

### B5 [mittel/stiller-fehler] Ablauf-Zweig von uebernehmen() ignoriert dk_prosp_extern -- Zustands-Welt am/nach t_a=n faellt auf Stamm-drx_bpfl zurueck

Block schicht-verankerung; `src/rechner_pipeline/bestand/migrationszugang.py:224`; Status: ERLEDIGT dc0aa42

e4230e9 hat den Ablauf-Sonderzweig (jahr >= mp.n) auf dk_ablauf = kern.verlaufszeile(mp.n).drx_bpfl umgebaut und dabei bewusst von der basis-Liste entkoppelt (teilt sich mit der Form bewusst keine Liste mehr) -- aber v.dk_prosp_extern (von 7325e87 zwei Stunden spaeter eingefuehrt) wird in diesem Zweig an keiner Stelle referenziert; die Abfrage auf dk_prosp_extern steht erst nach dem continue des Ablauf-Zweigs. Fuer eine Zustands-Welt-Police (Scheiben/Herabsetzung/Beitragsfrei), deren Verankerungszeitpunkt monate_ta genau auf oder nach dem nominalen Vertragsende faellt (monate_ta // 12 >= mp.n -- z.B. eine junge Erhoehungsscheibe, deren Uebernahme erst am Hauptvertrags-Ablauf stattfindet), wird dk_prosp nicht aus dem korrekt gerechneten Zustands-Wert gebildet, sondern aus der Deckungsrueckstellung des reinen Grund-Bausteins (mp ohne Scheiben/Reduktion) -- fuer eine Serien-Police fehlt damit der komplette Scheiben-Beitrag zur Ablaufleistung im Residuum, und selbst fuer Ein-Baustein-Welten ist es drx_bpfl statt des in fd359f1 als korrekte Basis identifizierten vx_mrv. Der resultierende Befund-Eintrag (parameter=None, also nicht im Journal, aber im zugangsbericht als befunde ausgewiesen) traegt ein falsches Residuum. Kein Test kombiniert dk_prosp_extern mit jahr>=mp.n: test_extern_gerechneter_prospektivwert_bestimmt_das_residuum verankert bei Monat 120 (weit vor Ablauf), der Ablauf-Test nutzt kein dk_prosp_extern -- die Kombination beider Zweige ist ungetestet.

### B6 [mittel/stiller-fehler] Anteils-Unerheblichkeit: Aequivalenzpruefung bundelt alle offenen RED-Positionen statt je Position zu unterscheiden

Block serien-ableitungen; `src/rechner_pipeline/bestand/migrationszugang.py:1218`; Status: ERLEDIGT a315094 (mit S9)

Bei mehreren Treffern prueft bestimme_serie_mit_kandidaten (Zeile 1218-1235) nur, ob ALLE Treffer-Kombinationen dieselbe GESAMTSTRUKTUR (grundsumme, scheiben) ergeben, und markiert dann PAUSCHAL alle Indizes aus `offen` als anteil_unbestimmt -- ohne zu pruefen, ob einzelne offene Positionen ISOLIERT durch die Beitrags-/Ankerprobe eindeutig bestimmt sind. Reproduziert (Python, gegen den echten Code): Ereignisse RED(Jahr1,offen)+ERH(3)+ERH(5)+RED(Jahr8,offen) mit wahren Anteilen f1=0.6 (vor der ersten Erhoehung, daher fuer die Struktur irrelevant) und f2=0.5 (nach den Erhoehungen, daher ueber den Jahresbeitrag EINDEUTIG bestimmbar) und Kandidaten (0.5,0.6,0.75): bestimme_serie_mit_kandidaten(..., jbrutto=<wahrer Beitrag>) liefert absetzungen=() und anteil_unbestimmt=(1, 8) statt des korrekten absetzungen=((8, 0.5),), anteil_unbestimmt=(1,). Der tatsaechlich bestimmbare Anteil des Jahres 8 wird also STILL VERWORFEN, nur weil er mit einer echten Unerheblichkeit (Jahr 1) im selben Kandidaten-Produkt kombiniert wird -- der Beleg in alt_absetzungen fehlt dann fuer eine Police, deren zweiter RED-Anteil eigentlich zweifelsfrei feststeht.

### B7 [mittel/stiller-fehler] quell_komponenten hat keine Obergrenze/Plausibilisierung gegen Dubletten

Block at-engine; `src/rechner_pipeline/gates/migrationssuite_lauf.py:226`; Status: ERLEDIGT 0a20b66

quell_komponenten = 1 + Anzahl ERH-Zeilen der Vorgeschichte (_serienzustand, PEX-Terminalzweig) fliesst ungeprueft in die Rundungstoleranz (_ok, komponenten-Skalierung: +0,005 EUR je Komponente). In aktuarieller_test.py Zeile 526 gibt es nur die Untergrenze `quell_komponenten < 1` -> Fehler; keine Obergrenze und kein Abgleich gegen z. B. eine plausible Bausteinzahl oder Duplikat-Erkennung in der Vorgeschichte (gleiche POLNR/DATUM/GEVO-Kombination zweimal geliefert). Eine fehlerhaft doppelt gelieferte ERH-Zeile in der registrierten Vorgeschichte (Lieferqualitaetsproblem, kein Programmierfehler) wuerde still eine zusaetzliche Toleranz-Stufe (0,005 EUR) gewaehren und so einen echten Residuenfehler unter die Nachweisgrenze druecken, statt als Datenanomalie aufzufallen. Kein Test prueft _serienzustand mit einer doppelten ERH-Zeile am selben Datum.

### B8 [mittel/stiller-fehler] Teilkuendigungs-Kern-Neubindung kompensiert nicht bei zwei RED-GeVos im selben Zeitraum

Block suite-gates; `src/rechner_pipeline/qa/migrationssuite.py:745`; Status: ERLEDIGT 23311b9

Fuer red_verfahren!=TEILKUENDIGUNG setzt der RED-Zweig red_monat (727, 751), wodurch die Wache 'if red_monat is not None' (573) jeden weiteren GeVo im Zeitraum -- inklusive eines zweiten RED -- hart als Befund abweist. Der TEILKUENDIGUNG-Zweig (735-747) setzt red_monat NIE, daher greift diese Sperre hier nicht (das ist laut Kommentar '...keine Folge-GeVo-Sperre' beabsichtigt fuer ERH/STO/TOD/ABL). Die Neubindung `kern = Rechenkern(replace(grund_mp, sum_insured=g.anteil * grund_mp.sum_insured))` rechnet aber jedesmal von grund_mp.sum_insured (dem URSPRUENGLICHEN Wert), nicht von kern.mp.sum_insured (dem bereits reduzierten Zwischenstand). Zwei RED/teilkuendigung-GeVos im selben Pruefzeitraum (z.B. anteil=0.8 bei Monat 60, dann anteil=0.6 bei Monat 120) fuehren NICHT zu einer Kette 0.8*0.6=0.48*S, sondern die zweite Buchung ueberschreibt die erste ersatzlos auf 0.6*S -- ein stiller Wert, der weder als Fehler noch als Pruefluecke sichtbar wird. test_teilkuendigung_gevo_fuehrt_zustandslos_fort deckt nur den Ein-GeVo-Fall ab.

### B9 [gering/stiller-fehler] Monatsreserve.rkw-Docstring (max(0, vx_mrv-stoab)) stimmt bei stoab_je_baustein=True nicht mehr

Block kern-verfahren; `src/rechner_pipeline/kern/produkte/klv.py:104`; Status: ERLEDIGT 5d635ee

Die Klassen-Doku von Monatsreserve.rkw sagt weiterhin 'max(0, vx_mrv - stoab)'. Mit stoab_je_baustein=True (2b35155, rechenkern.py vertrags_monatsreserve) wird rkw aber als SUMME der PRO BAUSTEIN auf 0 geklemmten Teil-RKW gebildet, waehrend stoab die volle (nicht auf die Baustein-Reserve begrenzte) Summe der Mindestabzuege bleibt. tests/test_kern_monatsreserve.py::TestStoabJeBaustein::test_baustein_unter_dem_mindestabzug_klemmt_bei_null belegt das explizit: `vertrag.rkw > max(0.0, vertrag.vx_mrv - vertrag.stoab)`. Fuer eine junge/kleine Erhoehungsscheibe, deren Reserve unter stoab_min liegt (z. B. Reserve 150 EUR bei stoab_min=200 EUR), summiert `stoab` die vollen 200 EUR, obwohl aus dieser Scheibe nur 150 EUR entnehmbar waren -- stoab+rkw != vx_mrv fuer den Vertrag. Wer sich auf den (jetzt falschen) Klassenkommentar verlaesst und rkw aus stoab/vx_mrv zurueckrechnet, bekommt einen zu niedrigen Wert.

### B10 [gering/stiller-fehler] plaus_verweigert nennt falschen Grund fuer Policen ausserhalb des gelieferten Auftragsbestands

Block at-engine; `src/rechner_pipeline/gates/aktuartest_lauf.py:682`; Status: ERLEDIGT 86e8ae0

`plausibilitaet` (Zeile ~657) wird ausschliesslich aus der registrierten Vorgeschichte gebildet (`betroffen = {POLNR : GEVO==art}`), unabhaengig davon, ob die Police ueberhaupt in `lieferung['vertraege']`/`auftraege` vorkommt. `vergeben = {a.police_id: bool(a.plausibilitaet) for a in auftraege}` (Zeile ~676) kennt nur tatsaechlich gebaute Auftraege; fuer eine Police, die im Plausibilitaets-Beleg genannt ist, aber gar nicht im gelieferten Bestand/Stichprobe steht, liefert `vergeben.get(p, False)` False, und da sie auch nicht in `zustandslos` steht (das nur ueber die for-Schleife der tatsaechlichen `lieferung['vertraege']' befuellt wird), erhaelt sie im Ergebnis-JSON `plausibilitaet_nicht_angewandt[p] = 'kein_herabsetzungs_anfangszustand'` (Zeile ~682f) statt des tatsaechlichen Grundes ('nicht in der Stichprobe'). Der Aktuar bekaeme eine irrefuehrende Begruendung im Bericht vorgelegt.

### B11 [gering/testluecke] Testluecke: dk_am_jahrestag und mehrfache Teilkuendigung im RED-Pfad ungetestet

Block suite-gates; `tests/test_migrationssuite.py:?`; Status: ERLEDIGT (a: 54f0a64, b: 23311b9)

Keiner der 884-985 (RED-Tests) und 1249-1276 (dk_am_jahrestag-Test) kombiniert dk_am_jahrestag=True mit einem mid-period RED-GeVo unter red_verfahren=prospektiv/mit_abzug -- genau die Mutation aus Befund 1 (v.monate_stichtag_2 statt dk_monat_2 in Zeile 812) waere durch keinen bestehenden Test gefangen. Ebenso testet test_teilkuendigung_gevo_fuehrt_zustandslos_fort (a5b86e7) nur EIN RED-GeVo pro Vertrag; eine Mutation, die bei einem zweiten RED-GeVo grund_mp.sum_insured durch kern.mp.sum_insured ersetzt (oder umgekehrt), aendert an keinem der beiden Testfaelle das Ergebnis und wuerde daher unbemerkt bleiben.

## Strittige Befunde (je ein Skeptiker widersprach; eigene Bewertung noetig)

### S1 [kritisch/stiller-fehler] gamma1-Lieferungseigenschaft nicht bis leite_erhoehung_ab durchverdrahtet

Block kern-verfahren; `src/rechner_pipeline/bestand/migrationszugang.py:1353`; Status: BESTAETIGT + ERLEDIGT 5b29cec

leite_erhoehung_ab() (aufgerufen aus gates/migrationssuite_lauf.py:408-410 fuer jede Police mit einem einzelnen ERH-Ereignis ohne belegten --erhoehungssatz) loest S_g*B_g + S_s*B_s = JBRUTTO, wobei B_s aus `Rechenkern(erhoehungs_scheibe(einheit, jahr, 1.0)).gross_premium_rate()` OHNE `gamma1_uebernehmen` kommt (Default False, Zeile 1353). Die Docstring der Funktion (Zeile 1325) haelt sogar fest: 'B_s, Modellpunkt mit versetzten Dauern und gamma1 = 0 nach der Tarifwerk-Regel' -- exakt die Annahme, die fc01663 fuer die zweite Baldrian-Lieferung als falsch bewiesen hat (Referenzvertrag-Residuum vorher -27,52 EUR). Obwohl migrationssuite_lauf.py seit fc01663 --scheiben-mit-gamma1/args.scheiben_mit_gamma1 kennt und es an baue_auftraege() sowie an leite_erhoehung_aus_satz_ab()-Zweig mit belegtem Satz weiterreicht, fehlt die Weitergabe genau im ELSE-Zweig (Zeile 408-410, kein --erhoehungssatz geliefert) an leite_erhoehung_ab(). Bei einem Lauf mit --scheiben-mit-gamma1 (dem fuer die zweite Lieferung korrekten Setting) wird fuer jede Police mit unbelegtem alten Dynamik-Ereignis die Grund-/Erhoehungssumme-Zerlegung dennoch mit der alten, falschen Rate berechnet -- die A-M4-Migrationscontrolling-Werte dieser Policen sind dadurch silently falsch, ohne dass irgendein Gate das anzeigt, weil das Ergebnis intern konsistent aussieht.

> Skeptiker: Nachgerechnet und bestaetigt. leite_erhoehung_ab() in migrationszugang.py (Zeilen 1310-1364) hat gar keinen gamma1-Parameter; Zeile 1352f. ruft erhoehungs_scheibe(einheit, jahr, 1.0) ohne gamma1_uebernehmen auf, was auf den Default False faellt. Die Docstring (Zeile 1333f.) behauptet "gamma1 = 0 nach der Tarifwerk-Regel" als feste Regel -- genau der Irrtum, den erhoehungs_scheibe()s eigene Docstring (rechenkern.py:133-147) unter Verweis auf den zweiten Baldrian-Lauf explizit widerlegt: gamma1_ue...

> Skeptiker: Der Code-Befund selbst stimmt: leite_erhoehung_ab (migrationszugang.py:1310-1325) nimmt keinen scheiben_mit_gamma1/gamma1_uebernehmen-Parameter an und ruft erhoehungs_scheibe(einheit, jahr, 1.0) ohne ihn auf (Default False), waehrend der ELSE-Zweig ohne --erhoehungssatz in migrationssuite_lauf.py (Zeilen ~408-410) sie unveraendert so aufruft. Die parallelen Pfade (bestimme_serie_mit_kandidaten, _serienzustand) fuehren das Flag dagegen korrekt bis zu erhoehungs_scheibe durch (Zeile 1192). Der Kon...

### S2 [kritisch/korrektheit] _zustands_dk_prosp ignoriert red_verfahren bei Herabsetzung -- immer prospektiv gerechnet

Block schicht-verankerung; `src/rechner_pipeline/gates/verankerung_belegen.py:127`; Status: OFFENER PUNKT (Fachentscheid Verankerungs-Verfahren, dev-docs/offene-punkte.md)

In _zustands_dk_prosp (eingefuehrt in 7325e87, Basis-Wahl in fd359f1 auf vx_mrv umgestellt) ruft der reduktion-Zweig ReduzierterVertrag.nach(kern, int(reduktion[0]), float(reduktion[1])) OHNE das Verfahren -- die Funktionssignatur nimmt gar kein red_verfahren-Argument entgegen (nur mp_kwargs, zustand, monate_ta, scheiben_mit_gamma1). ReduzierterVertrag.nach hat verfahren: str = PROSPEKTIV als Default (beitragsreduktion.py), wird also STETS mit PROSPEKTIV aufgerufen, unabhaengig vom --red-verfahren-CLI-Flag des Producers (das nur in anfangszustaende_je_police() einlaeuft, um den Zustand reduktion ueberhaupt zu erkennen, aber nicht mehr in _zustands_dk_prosp ankommt). Fuer eine Police, deren Herabsetzung dokumentiert unter dem Verfahren mit_abzug lief (das zweite PLV-Teilungsverfahren; CLAUDE.local nennt verfahren prospektiv/mit_abzug explizit als bitgenau zu erhaltende Vorgabe), rechnet dk_prosp_extern den geteilten Vertrag nach der falschen Split-Regel: reduziere() liefert unter mit_abzug ein anderes vs_neu/bfr_teil als unter prospektiv, der resultierende vx_mrv-Verlauf weicht ab, und die Weltendifferenz wird -- wie in den fruehen Korrekturen 18/20 diagnostiziert -- zum Phantom-Residuum, diesmal fuer mit_abzug-Policen. Beweis, dass es kein bewusstes Design ist: an anderer Stelle DERSELBEN Codebasis (migrationszugang.py Zeile 671, kalibriere_absetzung_aus_dk) wird ReduzierterVertrag.nach(kern, jahr, anteil, verfahren=verfahren) korrekt mit dem Verfahren durchgereicht -- der Producer vergisst es nur in _zustands_dk_prosp. Kein Test in tests/test_verankerung_belegen.py deckt den reduktion-Zweig ueberhaupt ab (0 Treffer fuer red_verfahren/mit_abzug/reduktion) -- die Mutation verfahren=MIT_ABZUG statt PROSPEKTIV faengt keine Suite.

> Skeptiker: Befund bestaetigt. `_zustands_dk_prosp` (verankerung_belegen.py:127-128) ruft `ReduzierterVertrag.nach(kern, int(reduktion[0]), float(reduktion[1]))` ohne `verfahren=`-Argument auf; `ReduzierterVertrag.nach` hat `verfahren: str = PROSPEKTIV` als Default (kern/beitragsreduktion.py:479-483), also rechnet der Aufruf immer prospektiv. Der `--red-verfahren`-Wert wird in main() zwar korrekt eingelesen und nach `anfangszustaende_je_police` durchgereicht (Zeile 370), dort aber nur zur Ableitung von vs_al...

> Skeptiker: Der Befund verwechselt zwei verschiedene Zwecke, unter denen ReduzierterVertrag.nach(...) im selben Modul mit unterschiedlichem verfahren aufgerufen wird, und schliesst daraus faelschlich auf einen Bug.  _zustands_dk_prosp liefert das Feld dk_prosp_extern der Uebernahme-Klasse (migrationszugang.py:103), dessen Feld-Docstring es explizit als 'Prospektiver Wert am t_a, EXTERN gerechnet' fuer Zustands-Welten beschreibt -- Zweck ist ausschliesslich, dieselbe BASIS (vx_mrv) und dieselbe ZUSTANDS-Welt...

### S3 [kritisch/stiller-fehler] Provenienzblock hasht vorgeschichte/zeilen/anker-erwartungswerte nicht -- Regler-Luecke trotz Bindungsversprechen

Block schicht-verankerung; `src/rechner_pipeline/gates/verankerung_belegen.py:393`; Status: OFFENER PUNKT (Provenienz-Haertung, dev-docs/offene-punkte.md)

Der Producer liest fuer die Zustandsableitung drei zusaetzliche Dateien (--vorgeschichte, --zeilen, --anker-erwartungswerte) und sie bestimmen ueber anfangszustaende_je_police() direkt scheiben/reduktion/beitragsfrei_seit_jahr, also dk_prosp_extern, also Residuum und Schichtparameter jeder Police. Im Provenienzblock (main(), eingaben-Dict) werden aber nur verankerung.parquet, bestand.parquet, optional merkmale.parquet und die Spez-Datei mit _sha256() gehasht; args.vorgeschichte/args.zeilen/args.anker_quelle landen ausschliesslich als Pfad-String unter parameter, ohne Hash. aktuartest_lauf._schichten() (46cb6a9) verifiziert bei --schicht auf ein abgeleitetes Artefakt nur die in prov[eingaben] gelisteten Dateien byte-fuer-byte; Dateien, die dort gar nicht auftauchen, werden nie geprueft. Damit kann die Vorgeschichte-CSV (oder die Zeilen-/Anker-Datei) nach Erzeugung des Belegs geaendert werden -- der Beleg besteht die nachgerechnete Bindung trotzdem, obwohl sein Inhalt nicht mehr zu den aktuellen Eingaben passt. Das unterlaeuft das in beiden Commit-Botschaften (46cb6a9, 7325e87) explizit behauptete Regler-Argument (ein Residuum, das der Pruefer selbst setzen koennte, waere kein Beweis, sondern ein Regler) -- ueber die Vorgeschichte-Datei kann der Operator sehr wohl einen freien Wert setzen, ohne dass die Bindungspruefung es bemerkt.

> Skeptiker: Befund bestaetigt. In verankerung_belegen.py main() (Zeilen 394-419) werden im Provenienzblock nur verankerung.parquet, bestand.parquet, optional merkmale.parquet und die Spez-Datei mit _sha256() gehasht und unter eingaben abgelegt. args.vorgeschichte und args.anker_quelle landen nur als Pfad-String unter parameter (Zeilen 411, 417); args.zeilen wird dort ueberhaupt nicht erfasst. Diese drei Dateien fliessen aber ueber anfangszustaende_je_police() (Zeilen 368-374) direkt in scheiben/reduktion/beitr...

> Skeptiker: Der Befund beschreibt korrekt, dass der Provenienzblock in verankerung_belegen.py (Zeile 393ff) nur verankerung.parquet, bestand.parquet, merkmale.parquet und die Spez-Datei hasht, waehrend args.vorgeschichte/args.zeilen/args.anker_quelle nur als Pfad-String unter "parameter" landen. Im realen Aufrufkontext ist das aber keine Regler-Luecke, weil vorgeschichte/zeilen/anker-erwartungswerte gar nicht ueber einen freien Dateipfad gelesen werden, sondern ausschliesslich ueber fall_mod.eingang_datei(f...

### S4 [kritisch/stiller-fehler] Herabsetzung-x-Schicht-Wache greift nicht bei Teilkuendigungs-RED im Pruefzeitraum

Block suite-gates; `src/rechner_pipeline/qa/migrationssuite.py:735`; Status: OFFENER PUNKT (Schicht ueber Teilkuendigung, dev-docs/offene-punkte.md)

Die in 900d990 eingefuehrte Wache (Zeilen 512-517) blockt Schicht nur, wenn `alt_rv is not None` ist -- alt_rv wird ausschliesslich aus v.reduktion (Anfangszustand VOR stichtag_1) gebaut. gates/migrationssuite_lauf.py.anfangszustaende_je_police ueberspringt (continue) aber gerade das Bauen eines RED-Anfangszustands, wenn red_verfahren==TEILKUENDIGUNG (900d990-Diff) -- v.reduktion bleibt fuer solche Vertraege also None, alt_rv bleibt None, die Wache feuert nie. Der in a5b86e7 eingefuehrte Zweig (735-747) bindet kern mid-period auf die gekuendigte f*S-Welt um, OHNE red_monat zu setzen; die Ausfuehrung faellt danach in den else-Zweig (821-845), wo dk2 += _schichtsumme(v, grund_mp, dk_monat_2) (841) den vollen, unskalierten Schichtwert unbedingt aufaddiert. Ein Vertrag mit sowohl schicht (Korrekturschicht aus der Verankerung) als auch einem RED-Teilkuendigungs-GeVo im Pruefzeitraum bekommt also den vollen Schichtwert auf die bereits um (1-f) reduzierte Reserve gebucht -- keine der beiden Codepfade (900d990-Wache, a5b86e7-Zweig) validiert diese Kombination, obwohl 900d990 explizit dieselbe Unvertraeglichkeit wie im aktuariellen Test dokumentiert. Kein Test in a5b86e7 oder 900d990 kombiniert schicht mit red_verfahren='teilkuendigung'.

> Skeptiker: Befund bestaetigt (nicht widerlegt).  Die Kernbehauptung stimmt bei Nachrechnung: Die Wache in Zeile 512-517 prueft ausschliesslich `alt_rv` (Anfangszustand `v.reduktion`, aus einer Herabsetzung VOR stichtag_1). `gates/migrationssuite_lauf.py` Zeile 384-392 ueberspringt bei `red_verfahren==TEILKUENDIGUNG` bewusst den Bau dieses Anfangszustands ("es gibt keinen geteilten Anfangszustand zu rekonstruieren") -- fuer eine im Pruefzeitraum liegende RED-Teilkuendigung bleibt `alt_rv` also `None`, die Wache feu...

> Skeptiker: Die Mechanik-Beschreibung des Befunds stimmt (Wache 512-517 prueft nur alt_rv; RED-in-Periode mit red_verfahren=TEILKUENDIGUNG setzt red_monat nicht; Ausfuehrung landet im else-Zweig 821-845 und addiert bei 841 den vollen _schichtsumme-Wert auf den mit dem reduzierten Kern (f*S) berechneten dk2). Das ist aber keine unvalidierte Kombination, sondern exakt das dokumentierte Design dieses Zweigs:  - Kommentar 741-744 im selben Codeblock sagt es ausdruecklich: "grund_mp bleibt die VERANKERUNGS-Welt: Sc...

### S5 [mittel/stiller-fehler] 'Harte Vorwaertsprobe' in leite_serie_aus_satz_ab ist tautologisch und erkennt einen falschen Satz/Anteil nicht

Block serien-ableitungen; `src/rechner_pipeline/bestand/migrationszugang.py:1073`; Status: BESTAETIGT + ERLEDIGT af2718a (Docstring/Meldung)

grundsumme_roh wird als erlsumme/gesamt_einheit(satz,anteile) bestimmt (Zeile ~1050), d.h. die Konstruktion GARANTIERT bereits algebraisch, dass grundsumme_roh*gesamt_einheit == erlsumme. probe = grundsumme_roh*grund_einheit (Zeile ~1071) und grundsumme_ist = erlsumme - sum(scheiben) unterscheiden sich danach NUR noch um die Cent-Rundung der Scheiben-Betraege -- eine Groesse, die strukturell klein bleibt (durch 0.01*(1+n) toleriert), UNABHAENGIG davon, ob der uebergebene satz oder die uebergebenen RED-Anteile tatsaechlich zutreffen. Der Docstring-Anspruch 'Reisst die Vorwaertsprobe trotzdem (Satz passt nicht zur Lieferung), ist das ein harter Fehler, keine stille Glaettung' ist dadurch fachlich unzutreffend: ein falscher --erhoehungssatz oder ein falsch nachgelieferter red_anteile_je_datum-Wert wuerde NICHT durch diese Probe abgefangen, sondern hoechstens durch eine negative/degenerierte Grundsumme (die einzige echte Fail-Bedingung) oder erst spaeter durch Bewertungs-Residuen im aktuariellen Test. Fuer eine GESCHLOSSENE Serie (alle RED-Anteile bereits geliefert, kein offen -> kein Aufruf von bestimme_serie_mit_kandidaten) existiert dann gar keine zweite, unabhaengige Gleichung mehr, die einen falschen satz oder falsch gelieferten Anteil aufdecken koennte.

> Skeptiker: Befund bestaetigt durch Nachrechnen am echten Code (src/rechner_pipeline/bestand/migrationszugang.py:1050-1073). grundsumme_roh = erlsumme/gesamt_einheit macht probe (=grundsumme_roh*grund_einheit) und grundsumme_ist (=erlsumme - sum(scheiben)) algebraisch identisch bis auf die Cent-Rundung der Scheiben, unabhaengig vom Wert von satz oder RED-Anteil. Empirischer Test mit den echten Testfixtures aus tests/test_migrationszugang.py: leite_serie_aus_satz_ab mit satz in {0.03,0.04,0.06,0.07,0.10,0.20...

> Skeptiker: Die Algebra des Befunds stimmt: grundsumme_roh wird als erlsumme/gesamt_einheit(satz,anteile) konstruiert, daher ist probe (Z.1071) gegen grundsumme_ist bis auf Cent-Rundung tautologisch erfuellt -- unabhaengig davon, ob satz oder die red_anteile_je_datum-Werte inhaltlich zutreffen. test_fehlerpfade_fail_fast (tests/test_migrationszugang.py:769) bestaetigt das indirekt: der einzige dort provozierte Fehlerpfad ist bereits die Range-Pruefung `0<satz<1` (satz=1.5), es gibt keinen Test, der die "Vorw...

### S6 [mittel/korrektheit] Komponentenskalierung fehlt im RED-Folgestichtag-Vergleich

Block suite-gates; `src/rechner_pipeline/qa/migrationssuite.py:810`; Status: BESTAETIGT + ERLEDIGT 54f0a64 (mit B3)

Korrektur 21 (71213a6) hat komponenten= an dk_stichtag_1 (Zeile 534-536), bjb_stichtag_1 (544) und den regulaeren dk_stichtag_2-Zweig (842-845) angehaengt, den Aufruf in Zeile 810-814 (reduziert.monatsreserve(...) im red_monat-Zweig) aber ausgelassen -- _vergleich faellt hier auf komponenten=1 zurueck. Bei einem Vertrag mit v.quell_komponenten=6 (kollabierte Quell-Bausteine) und einem mid-period RED unter prospektiv/mit_abzug betraegt die tatsaechlich zulaessige Toleranz 0.02+5*0.005=0.045, der Code prueft aber nur mit 0.02 -- ein Residuum von z.B. 0.03 EUR (dieselbe Groessenordnung wie die vier in Korrektur 21 behobenen Faelle) faellt hier faelschlich durch, obwohl dieselbe Fehlerfortpflanzung bei einem baugleichen Vertrag ohne RED korrekt toleriert wuerde. test_red_mit_anteil_rechnet_den_folgestichtag deckt nur den quell_komponenten=1-Fall ab.

> Skeptiker: Befund bestaetigt. In migrationssuite.py Zeile 810-814 (RED-Folgestichtag, reduziert.monatsreserve) fehlt tatsaechlich der komponenten=-Parameter im _vergleich-Aufruf, waehrend alle anderen DK-Vergleiche (Zeile 534-536 dk_stichtag_1, 539-545 bjb_stichtag_1, 842-845 der regulaere dk_stichtag_2-Zweig) ihn tragen (komponenten=max(1+len(scheiben), v.quell_komponenten or 1)). Da _vergleich() komponenten=1 als Default hat (Zeile 264) und die Toleranz ABS_TOL + _CENT_UNSCHAERFE*max(0,komponenten-1) ist ...

> Skeptiker: Die Beobachtung ist technisch richtig: Zeile 810-814 (reduziert.monatsreserve im red_monat-Zweig) haengt tatsaechlich kein komponenten= an, faellt also auf den Default 1 zurueck, waehrend die Nachbarzweige (534-536, 544, 842-845) v.quell_komponenten einrechnen. Der beschriebene Fehlerfall ist aber im realen Betrieb nicht erreichbar, weil quell_komponenten und die Bedingung, die den RED-Zweig ueberhaupt verhindert, an derselben Quelle haengen:  - quell_komponenten wird im gesamten Produktionscode...

### S7 [mittel/stiller-fehler] Gate-Nachrechnung der Komponententoleranz vertraut unbegrenzt dem selbstgemeldeten Zaehler

Block suite-gates; `src/rechner_pipeline/gates/abnahmebericht.py:128`; Status: OFFENER PUNKT (Zaehler-Obergrenze, dev-docs/offene-punkte.md)

_suite_vergleich_ok (1bb4e3d) liest `pruefung.get('komponenten', 1)` direkt aus dem von qa.migrationssuite selbst erzeugten Suite-Report und benutzt ihn ungeprueft zur Toleranzskalierung -- es gibt keine Gegenpruefung gegen eine unabhaengig herleitbare Obergrenze (z.B. 1+len(scheiben) oder quell_komponenten aus der Lieferung/TransformationsSpec). tests/test_abnahmebericht.py::test_gate_rechnet_die_komponentenskalierte_toleranz_nach demonstriert das selbst: Setzt man in einer manuell praeparierten Suite-Datei p['komponenten']=6, kippt ein Urteil, das mit komponenten=1 fehlschlagen wuerde, in 'ok'. Die Commit-Botschaft von 1bb4e3d bezeichnet die Nachrechnung als 'unabhaengig', unabhaengig ist aber nur die isclose-Formel -- der Eingabewert komponenten selbst stammt aus genau der Quelle, die das Gate eigentlich gegenpruefen soll. Ein Zaehl-Bug in _vergleich (oder eine manipulierte/beschaedigte Suite-Datei) mit ueberhoehtem komponenten wuerde vom Gate anstandslos bestaetigt.

> Skeptiker: Befund bestaetigt. `_suite_vergleich_ok` (abnahmebericht.py:125-140) liest `pruefung.get("komponenten", 1)` unmittelbar aus dem zu pruefenden Suite-JSON und benutzt ihn zur Toleranzaufweitung, ohne ihn gegen eine unabhaengig herleitbare Groesse (z.B. `1+len(scheiben)` oder `quell_komponenten`, wie sie qa/migrationssuite.py:533/844 tatsaechlich berechnet) gegenzupruefen. `_PRUEFUNG_FELDER` (Zeile 1016) listet "komponenten" nicht einmal als Pflichtfeld, und im gesamten Gate-Modul kommt "komponente...

> Skeptiker: Der Befund beschreibt eine reale Eigenschaft des Codes (die Toleranzskalierung stuetzt sich auf `pruefung["komponenten"]` aus der Suite-Datei), aber keinen stillen/unbeabsichtigten Fehler, sondern eine zweifach dokumentierte, bewusste Entwurfsentscheidung:  1. In qa/migrationssuite.py:279-282 steht am Ursprungsort des Feldes explizit: "Zaehler fuer die unabhaengige Nachrechnung des Gates (gates.abnahmebericht) -- ohne ihn koennte es die skalierte Toleranz nicht nachvollziehen und verwuerfe korre...

### S8 [gering/testluecke] Keine Testabdeckung fuer TEILKUENDIGUNG im beitragsfreien Nachlauf (t <= jahr <= n)

Block kern-verfahren; `tests/test_beitragsreduktion.py:449`; Status: ERLEDIGT b99cbbc (Test in B4 enthalten)

TestTeilkuendigung deckt nur jahr=10 bei KLV_DEFAULT (t=20, n=30) ab, also ausschliesslich die beitragspflichtige Phase. Die Mutation 'entferne oder aendere die jahr>=mp.t-Pruefung fuer verfahren==TEILKUENDIGUNG in _pruefe_eingaben' (Fund oben) wuerde von der Suite nicht erkannt, weil kein Test reduziere(kern, jahr, anteil, verfahren=TEILKUENDIGUNG) mit t<=jahr<=n aufruft -- weder um das jetzige harte Scheitern zu belegen noch um zu pruefen, ob dieses Scheitern fachlich gewollt ist.

> Skeptiker: Nachgerechnet: `_pruefe_eingaben` (beitragsreduktion.py:183-216) wendet die Wache `jahr >= mp.t` uniform auf ALLE Verfahren an, auch auf TEILKUENDIGUNG -- es gibt keine verfahrenspezifische Ausnahme oder einen zweiten Guard, der die Teilkuendigung im beitragsfreien Nachlauf separat behandelt. Der Docstring von `_pruefe_eingaben` begruendet die gemeinsame Wache nur mit "ungeteilter vs. geschichteter Weg", nicht mit einer Verfahrensunterscheidung.  grep ueber tests/test_beitragsreduktion.py bestaetigt:...

> Skeptiker: Die gemeinsame Wache jahr>=mp.t in _pruefe_eingaben ist laut Docstring (Zeilen 186-197) bewusst fuer JEDEN Verfahrensweg gleich - Reaktion auf einen frueheren Bug, bei dem der geschichtete Weg klaglos ungueltige Eingaben (u.a. Jahr nach Beitragsende) durchliess. Die Uniformitaet ist also explizit gewollt, nicht uebersehen. Wichtiger: reduziere(..., verfahren=TEILKUENDIGUNG) wird produktiv gar nicht mit jahr>=t erreicht.  (1) migrationszugang.py::leite_absetzung_ab/leite_pex_ursprungssumme_ab ruf...

### S9 [gering/testluecke] Testluecke: gemischte Bestimmbarkeit mehrerer offener RED-Anteile in einer Serie ist ungetestet

Block serien-ableitungen; `tests/test_migrationszugang.py:?`; Status: ERLEDIGT a315094 (Test in B6 enthalten)

TestSerienKandidatenBestimmung deckt nur Faelle mit GENAU EINER offenen RED-Position ab (Treffer, Fehlschlag, Anker-Diskriminierung, vollstaendige Unerheblichkeit, uneindeutige Strukturen). Kein Test hat zwei offene RED-Positionen, von denen nur eine strukturell invariant und die andere eindeutig bestimmbar ist -- genau das Muster aus Befund 2 (RED vor der ersten Erhoehung + RED danach). Die Mutation 'markiere bei mehrdeutigem Treffer immer ALLE offenen Positionen als unbestimmt statt je Position gegen die anderen Treffer zu pruefen' wuerde von der bestehenden Suite (1502 passed) nicht gefangen.

> Skeptiker: Befund bestaetigt (nicht widerlegt) -- und der Nachrechnung zufolge sogar staerker als eine reine Testluecke: das aktuelle Verhalten in bestimme_serie_mit_kandidaten (src/rechner_pipeline/bestand/migrationszugang.py, Zeilen 1218-1235) prueft bei mehreren Treffern die Struktur-Gleichheit nur GESAMTHAFT ueber grundsumme+scheiben, nie je Position. offene_jahre wird unbedingt aus ALLEN Eintraegen von `offen` gebildet (Zeile 1228) und komplett aus absetzungen entfernt, unabhaengig davon, ob einzelne of...

> Skeptiker: Nachvollzogen mit einem konkreten Repro (RED(1, unbekannt) vor der ersten ERH + RED(5, unbekannt) danach, Kandidaten (0.5, 0.6, 0.75), Beitragsprobe): Das Ergebnis ist tatsaechlich anteil_unbestimmt=(1, 5) statt (1,) -- die Funktion markiert bei mehrdeutigem Treffer wirklich ALLE offenen Positionen der Kombination, nicht je Position einzeln gegen die anderen Treffer geprueft (Zeilen 1218-1235 in migrationszugang.py). Das ist aber kein unentdeckter Fehler, sondern schon der IST-Zustand des Codes --...

## Verworfen (beide Skeptiker widerlegten)

2 Rohbefunde; Details im Roh-Journal des Laufs (nicht versioniert).
